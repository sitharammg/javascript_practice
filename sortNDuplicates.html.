<!DOCTYPE html>
<html><!--11-->
    
    <head>
        <title>Sort and Remove duplicates</title>
        <link rel="stylesheet" href="style.css">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script>
    </head>
<body>
  <form method="post" name="myForm" class="form-group ">
    <pre>Sort and Remove duplicates from given input</pre>
    <h4>Please input Numbers and/or Strings and press OK to Sort and remove duplicates: </h4>
    <div class="form-group ">
        <div><input id="number" type="text" class="form-control"></div>
        <div><button type="button" class="btn btn-primary" id="my-btn" onclick="myValidation()">OK</button></div>
    </div>    
    <p id="disp"></p>
  </form>
    <script>
     
      function myValidation(){
        var n ="";
        n+= document.getElementById("number").value;
        var re=/^[0-9A-z,\s]+$/;
        if(n == ""){
          text="Number not provided";
        }
        else if(!(n.match(re))){
            text="Enter a valid number or String";
           
        } else { 
          text="Accepted  " + n;
          var arr=n.split(",");
          if(!(arr instanceof Array)){text = "Not an array";}
          else{text= arr.filter((item, index)=> (arr.indexOf(item)>=index))
          text.sort();
          }
        }
        document.getElementById("disp").innerHTML = text;
      }
    </script>
</body>
</html>